export enum EventNameSpace {
  WHITEBOARD = 'whiteboard',
  SCROLLBAR_VERTICAL = 'scrollbar_vertical',
  SCROLLBAR_HORIZONTAL = 'scrollbar_horizontal',
}

export enum EmitterEventName {
  RESIZE_CHANGE = 'resize-change',
}
